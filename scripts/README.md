# Maintenance scripts

- `purge-game-history.ps1` is a one-time fallback for removing the proprietary local game directory from rewritten Git history. The repository also contains a one-time GitHub Actions purge workflow used during the pre-Phase-3 cleanup. Keep this script only as a recovery/manual reference after that workflow is removed.
